# Iceberg Catalog Configuration (Nessie)
spark.sql.catalog.nessie = org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.nessie.catalog-impl = org.apache.iceberg.nessie.NessieCatalog
spark.sql.catalog.nessie.uri = http://nessie:19120/api/v1
spark.sql.catalog.nessie.ref = main
spark.sql.catalog.nessie.authentication.type = NONE
spark.sql.catalog.nessie.warehouse = s3a://modern-cdc-bucket/iceberg-warehouse
spark.sql.catalog.nessie.io-impl = org.apache.iceberg.aws.s3.S3FileIO

# S3 / MinIO Connection
spark.hadoop.fs.s3a.endpoint = http://minio:9000
spark.hadoop.fs.s3a.access.key = minio_admin
spark.hadoop.fs.s3a.secret.key = minio_password
spark.hadoop.fs.s3a.path.style.access = true
spark.hadoop.fs.s3a.impl = org.apache.hadoop.fs.s3a.S3AFileSystem

# Performance & Extensions
spark.sql.extensions = org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

# Spark Ivy Cache (Fix for download failures)
spark.jars.ivy = /tmp/spark-ivy-cache

